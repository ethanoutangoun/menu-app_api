/**
 * Service for processing individual reviews
 * This is where you can add your custom review processing logic
 */

const processReview = (review) => {
  // For now, just return the same review object
  return review;
};

module.exports = {
  processReview,
};
